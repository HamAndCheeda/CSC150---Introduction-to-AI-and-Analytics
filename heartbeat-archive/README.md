# Heartbeat Archive

Double-click `run-html.bat` to open `index.html` in your default browser.

If you want to run a local web server instead, use any static file server and point it at this folder.
